# Copy to config/local.env only when local overrides are required.
# Do not commit secrets.
#
# Producer symbols and intervals are declared per service in infra/compose/streaming.yml.
PRODUCER_POLL_SECONDS=15
